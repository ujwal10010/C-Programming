#include<stdio.h>
int main(){
    int num,rev=0;
    
    scanf("%d",&num);
    if(num>=0 && num<=9)
        printf("%d",num);
    else{
        int num1 = num;
        while(num !=0){
            int rem = num%10;
            rev = rev*10+rem;
                num = num / 10;
        }
        if(num1 == rev)
            printf("Palindrome");
        else
            printf("Not Palindrome");
    }
    
    
    
    return 0;
}

 #include<stdio.h>
int main(){
int i,x,sum=0;
    scanf("%d",&x);
    int list[x];
    for(i=0;i<x;i=i+1){
        scanf("%d",&list[i]);
    }
    for(i=0;i<x;i++){
        sum=sum+list[i];
       
    }
    printf("%d ",sum);
    return 0;
}
